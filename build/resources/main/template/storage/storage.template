package org.jarogoose.calculator.investor.storage;

import java.util.Optional;
import org.springframework.data.mongodb.repository.MongoRepository;
import org.springframework.stereotype.Repository;

@Repository
interface {{class-name}}Storage extends MongoRepository<{{class-name}}Entity, String> {
  Optional<{{class-name}}Entity> findByEmail(String login);
}
