server:
  port: 8000

spring:
  data:
    mongodb:
      # authentication-database: 'arcadmin'
      username: 'arcadmin'
      password: 'admin'
      database: archigen
      port: 27017
      host: localhost
      auto-index-creation: true
